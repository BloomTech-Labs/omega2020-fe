import React from 'react';

const useValidation = (cellData, cellId) => {
    // if cellData = 1-9 and is unique to row (i)
    // if cellData = 1-9 and is unique to column (j)
    // if cellData = 1-9 and is unique to field

}

export default useValidation;